# Variables

PATH=$HOME/bin:$PATH
EDITOR='vim'
VISUAL='gvim'

export LESS='-g -i -M -R -S -w -z-4'
export PATH EDITOR VISUAL

#[[ -s "/home/pablo/.rvm/scripts/rvm" ]] && source "/home/pablo/.rvm/scripts/rvm"  # This loads RVM into a shell session.

